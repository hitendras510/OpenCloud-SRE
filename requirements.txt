# ─── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0
pyyaml>=6.0.1                  # utils/config_loader.py

# ─── LLM / Agents (vendor-agnostic, no OpenAI) ───────────────────────────────
huggingface_hub>=0.23.0        # InferenceClient for Llama-3 (dataset gen + eval)
transformers>=4.41.0
datasets>=2.19.0
accelerate>=0.30.0             # BUG-FIX: required by transformers device_map="auto"
trl>=0.9.0                     # BUG-FIX: SFTConfig added in 0.9.0 (was 0.8.6)

# ─── LangGraph / LangChain ────────────────────────────────────────────────────
langgraph>=0.1.19
langchain-core>=0.2.0

# ─── OpenEnv FastAPI Server ───────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
requests>=2.31.0
pydantic>=2.7.0

# ─── FAISS (DNA Memory) ───────────────────────────────────────────────────────
faiss-cpu>=1.8.0

# ─── UI ───────────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
typing-extensions>=4.11.0

# ─── Experiment Tracking ──────────────────────────────────────────────────────
wandb>=0.17.0

# ─── GPU-only (install separately on CUDA VMs) ────────────────────────────────
# unsloth                      # install per Unsloth instructions: pip install unsloth
# bitsandbytes>=0.43.0         # auto-installed by setup_env.sh when CUDA is detected
